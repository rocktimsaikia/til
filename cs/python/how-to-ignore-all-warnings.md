# How to ignore all warnings in Python?

We can ignore all the warnings in our program using the `warnings` module.

```python
import warnings

# Ignore all warnings
warnings.filterwarnings("ignore")
```
